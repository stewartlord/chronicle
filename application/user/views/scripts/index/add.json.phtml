<?
// assemble json response
$response            = array();
$response['form']    = $this->render('index/add.partial.phtml');
$response['errors']  = isset($this->errors) ? $this->errors : null;
$response['message'] = isset($this->message) ? $this->message : '';

print $this->json($response);